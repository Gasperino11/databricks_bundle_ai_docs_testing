name: Review Ad-Hoc DAB Documentation

on:
  workflow_dispatch:
    inputs:
      folder_path:
        description: "Path to the DAB folder to review (e.g., data_eng/my_pipeline). Must contain databricks.yml and README.md."
        required: true

jobs:
  review-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate DAB folder
        run: |
          FOLDER="${{ github.event.inputs.folder_path }}"
          if [ ! -f "$FOLDER/databricks.yml" ]; then
            echo "Error: No databricks.yml found in '$FOLDER'"
            exit 1
          fi
          if [ ! -f "$FOLDER/README.md" ]; then
            echo "Error: No README.md found in '$FOLDER'"
            exit 1
          fi
          echo "Found DAB at: $FOLDER"

      - name: Dispatch Copilot review agent
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.COPILOT_PAT }}
          script: |
            const folderPath = '${{ github.event.inputs.folder_path }}';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;

            const body = [
              "## Task: Review DAB Documentation",
              "",
              `This task was triggered manually via workflow run [#${context.runId}](${runUrl}).`,
              "",
              "Please use the **review-dab-documentation** skill (`.github/agents/skills/review-dab-documentation.md`) to review the Databricks Asset Bundle below.",
              "The original README template can always be found at `templates/README_TEMPLATE.md`.",
              "",
              "### DAB to Review",
              `- \`${folderPath}\``,
              "",
              "### Instructions",
              "1. Follow the `review-dab-documentation` skill for the DAB path above.",
              "2. Write a `REVIEW.md` file in the DAB root summarizing all findings.",
              "3. Commit the `REVIEW.md` to the default branch."
            ].join('\n');

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Review DAB documentation: ${folderPath}`,
              body,
              assignees: ['copilot']
            });
